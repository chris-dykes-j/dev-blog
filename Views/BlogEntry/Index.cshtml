@model DevBlog.Models.BlogEntry;

<div class="intro">
    <h1 id="page-title">
        @Model.Title
    </h1>
    <p class="page-description">
        @Model.Description
    </p>
    <p class="page-description">
        @Model.Date
    </p>
</div>

<div class="content-container">
    <p id="page-content">@Html.Raw(Model.Content)</p>
</div>